# TAFFO CI containers

A collection of Docker containers with the dependencies for the [TAFFO](https://github.com/TAFFO-org/TAFFO) project.

At the moment, the CI configuration is disabled and each image needs to be manually built and pushed to Docker Hub.
